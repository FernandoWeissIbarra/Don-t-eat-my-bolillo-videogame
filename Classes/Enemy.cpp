/*
#include "Enemy.h"

bool Enemy::init() {
    // Inicialización básica del enemigo
    if (!Sprite::init()) {
        return false;
    }
    // Configurar propiedades iniciales del enemigo aquí (imagen, tamaño, etc.)
    this->setTexture("enemy.png"); // Asegúrate de tener esta textura
    return true;
}
*/
